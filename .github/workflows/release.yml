name: Release

on:
  workflow_dispatch:
  release:
    types: [created]

jobs:
  release:
    runs-on: ubuntu-24.04
    environment: pypi
    permissions:
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up deps
        uses: ./.github/actions/setup_deps
        with:
          include_node: "yes"

      - name: Build frontend
        run: just prod

      - name: Build Python package
        run: |
          uv build tierkreis
          uv build tierkreis_visualization
          uv build tierkreis_workers/*/

      - name: Publish package distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          verbose: true
          skip-existing: true
