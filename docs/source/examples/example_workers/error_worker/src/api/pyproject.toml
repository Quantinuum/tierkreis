[project]
name = "error-worker-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "tierkreis",
]

[tool.uv.sources]
tierkreis = { path = "../../../../../../../tierkreis", editable = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel.force-include]
"stubs.py" = "error_worker.py"
