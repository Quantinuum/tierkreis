[project]
name = "openssl-worker"
version = "0.2.0"
requires-python = ">=3.12"
dependencies = [
    "tierkreis",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel.force-include]
"api.py" = "openssl_worker.py"
