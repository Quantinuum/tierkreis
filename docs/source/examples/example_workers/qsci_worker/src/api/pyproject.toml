[project]
name = "qsci-worker-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "tierkreis",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel.force-include]
"stubs.py" = "qsci_worker.py"
