[project]
name = "tkr_pytket_worker"
version = "0.1.6"
description = "A tierkreis worker for pytket."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "tierkreis",
    "pytket",
    "qnexus",
    "pytket_qirpass",
]

[project.optional-dependencies]
backend-qiskit = [
    "pytket-qiskit",
]
backend-quantinuum = [
    "pytket-quantinuum",
]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.sdist]
include = [
  "*.py",
]
[tool.hatch.build.targets.wheel]
include = [
  "*.py",
]

[tool.uv.sources]
tierkreis = { workspace = true }
pytket-qiskist = { git = "https://github.com/Quantinuum/pytket-qiskit" }
pytket-quantinuum = { git = "https://github.com/Quantinuum/pytket-quantinuum" }

[project.scripts]
tkr_pytket_worker = "main:main"
