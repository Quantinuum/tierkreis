[project]
name = "tkr-gpu-worker"
version = "0.2.1"
description = "The api for the Tierkreis gpu worker."
requires-python = ">=3.12"
authors = [ {name = "Quantinuum Tierkreis Team"} ]
dependencies = [
    "tierkreis",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel.force-include]
"api.py" = "gpu_worker.py"
