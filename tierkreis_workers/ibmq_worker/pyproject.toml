[project]
name = "tkr-ibmq-worker-impl"
version = "0.2.1"
description = "A Tierkreis worker for compilation of ibmq backends."
authors = [ {name = "Quantinuum Tierkreis Team"} ]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "tierkreis",
    "pytket",
    "pytket-qiskit",
    "tkr-ibmq-worker"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"


[tool.uv.sources]
tkr-ibmq-worker = { workspace = true }

[project.scripts]
tkr-ibmq-worker = "tkr_ibmq_worker_impl.main:main"
